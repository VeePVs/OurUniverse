import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop: 10,
        marginBottom: 10,
        width: '40%',
        borderColor: '#000',
        borderWidth: 1,
        height: 30,
        borderRadius: 5,
        justifyContent: 'center',
    },
    textContainer: {
        textAlign: 'center',
    }
})